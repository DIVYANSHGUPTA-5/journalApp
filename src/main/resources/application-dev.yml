spring:
  redis:
    host: localhost
    port: 6379

  data:
    mongodb:
      uri: ${MONGO_URI}
      database: journaldb
      auto-index-creation: true

  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: 8735639b23a821
    password: 633b72bb2d1a5a
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - email
              - profile
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            user-name-attribute: sub


weather:
  api:
    key: 5c41ec298e9621062e1eade751bd6001

    spring:
      mail:
        properties:
          mail:
            smtp:
              connectiontimeout: 5000
              timeout: 5000
              writetimeout: 5000